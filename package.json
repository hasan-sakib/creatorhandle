{
  "name": "fastapi-full-stack-template",
  "private": true,
  "workspaces": [
    "frontend/creator-workspace",
    "frontend/creator-site"
  ],
  "scripts": {
    "dev": "bun run --filter creator-workspace dev",
    "lint": "bun run --filter creator-workspace lint",
    "test": "bun run --filter creator-workspace test",
    "test:ui": "bun run --filter creator-workspace test:ui"
  }
}
